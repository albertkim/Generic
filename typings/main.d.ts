/** Importing an external class {User} into this file prevents this 
 *  definition from being properly detected as a pure definition file.
 */

declare namespace Express {
  export interface Request {
    user?: any
  }
}
